
import java.lang.classfile.Signature;

class ListNode{
    int val;
    ListNode next;
    public static void listNode(int val){
        this.data = val;
        this.next = null;
    }
    public stack
    public static void main(String[] args) {
        ListNode nodel = new ListNoded(1);
        ListNode ptr = nodel;
        while(ptr!=null){
            System.out.println(ptr.val);
            ptr = ptr.next;
        } 
        System.out.println("After insertion 5");
        ptr = nodel;
        while(ptr!=null){
            System.out.println(ptr.val);
            ptr = ptr.next;
        }
    }
}