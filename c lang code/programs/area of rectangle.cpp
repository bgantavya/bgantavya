#include <stdio.h>
int main(){
	//area of rectangle calculator//
	float length,breadth,area;
	printf("enter the value of length and breadth");
	scanf("%f",&length);
		scanf("%f",&breadth);
	area= length*breadth;
	printf(" the area of the rectangle with length %f and breadth %f is %f",length,breadth,area);
return 0;}
