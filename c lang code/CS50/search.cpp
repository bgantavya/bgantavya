#include<stdio.h>
int main(void)
{
    printf("hello!")
    string strings[] = {"battleship", "boat", "plane", "jet", "tank", "fighter"};
    scanf("%c",&s);
    for(int i = 0; i < 6; i++){
        if (strings[i]==s){
            printf("found\n %c",strings[i])
        }
    }
    return 0;
}